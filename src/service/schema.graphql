#import SampleJson, Query.sampleJsons, Mutation.createSampleJson from "./generated/prisma.graphql"

scalar Json

type Query {
  allFoos(name: String): [Foo]!
}

type Foo {
  name: String
  secretFoo: String
}
